import * as deprecated from './deprecated.json';
import * as unnecessary from './unnecessary.json';
import * as power from './power.json';

export var mapcss = {
	rules : {
		deprecated: deprecated,
		unnecessary: unnecessary,
		power: power
	}
};